const circle = require("./circle")

const area = circle.circleArea(4)
const circumference = circle.circleCircumference(5)
const pi = circle.circlePI;

console.log(`Area : ${area}\nCircumference : ${circumference}\nPI = ${pi}`)
